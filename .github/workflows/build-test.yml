name: Build and Test the site without deployment

# Controls when the workflow will run
on:
  # Triggers the workflow on pull requests for the main branch
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  jekyll:
    runs-on: ubuntu-latest
    steps:
      # Checkout git branch
      - name: Checkout
        uses: actions/checkout@v3

      # Automatic setup of Ruby environment and dependencies
      - name: Perform Ruby Setup
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.7
          bundler-cache: true

      # Build the site and serve it locally
      - name: Build jekyll site
        run: bundle exec jekyll serve -w # Do not watch and rebuild, as tests shouldn't alter the source anyways

      # Run static test scripts
      - name: Run Test Scripts
        run: bash _site/run_tests.sh

